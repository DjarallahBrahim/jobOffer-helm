apiVersion: v1
kind: Service
metadata:
  name: {{ include "producer-service-fullName" . }}
  labels:
    app: {{ include "producer-service-fullName" . }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: {{ include "producer-service-fullName" . }}
